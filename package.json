{
  "name": "node-robot",
  "author": "0chroma <will@0chroma.org>",
  "description": "An adapter-agnostic framework for controlling robots with node. Has adapters for controlling EV3 bricks over Bluetooth.",
  "repository": "https://github.com/0chroma/node-robot.git",
  "version": "0.0.5",
  "scripts": {
    "test": "echo -e '\\E[47;31m'\"\\033[1mNOTE: If SERIAL_PORT env variable isn't set, some tests will be pending\\033[0m\"; ./node_modules/mocha/bin/mocha $(find test -name '*.js')"
  },
  "dependencies": {
    "serialport": "^1.4.0",
    "underscore": "^1.6.0"
  },
  "engines": {
    "node": ">=0.10"
  },
  "devDependencies": {
    "mocha": "^1.20.1"
  }
}
